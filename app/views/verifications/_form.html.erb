<%= form_for(@verification) do |f| %>
  <% if @verification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@verification.errors.count, "error") %> prohibited this verification from being saved:</h2>

      <ul>
      <% @verification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr>
      <td><%= f.label :name %></td>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr class="field">
      <td><%= f.label :phone %></td>
      <td><%= f.text_field :phone %></td>
    </tr>
    <tr class="field">
      <td><%= f.label :email %></td>
      <td><%= f.email_field :email %></td>
    </tr>
    <tr class="field">
      <td><%#= f.label :adviser_id %></td>
      <td><%= f.hidden_field :adviser_id %></td>
    </tr>
    <tr class="field">
      <td><%#= f.label :user_id %></td>
      <td><%= f.hidden_field :adviser_user_id %></td>
    </tr>
  </table>
  <p class="actions">
    <%= f.submit %>
  </p>
<% end %>
